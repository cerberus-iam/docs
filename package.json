{
  "name": "@cerberus-iam/docs",
  "version": "0.1.0",
  "description": "Cerberus IAM API Documentation",
  "private": true,
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "start": "npx --yes serve -s .vitepress/dist -l $PORT"
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}
